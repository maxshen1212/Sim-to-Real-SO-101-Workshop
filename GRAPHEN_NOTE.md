# 觀念筆記

通用參考。底下的數字都是為了好心算而編的整數,不是本專案的實際值。

---

# 1. Servo Encoder

伺服馬達裡的位置感測器。它回報的是 **counts(編碼器刻度)**,不是角度。
N-bit 編碼器把整個行程切成 `2^N` 格。

**範例** —— 12-bit 編碼器,行程 360°:

```
格數      = 2^12 = 4096          → 有效範圍 0…4095
解析度    = 360° / 4096 = 0.088° 每格
讀到 1024 = 1024 × 0.088° = 90°
```

兩個推論:

- **counts 是硬體單位。** 同一個物理角度在不同顆伺服上會讀到不同的 count,
  因為編碼器的零點取決於舵機臂當初怎麼裝上去的。
- **解析度就是你的雜訊底線。** 這裡命令不可能細於 0.088°,所以要求 0.01° 精度的控制迴圈是在要求空氣。

---

# 2. Normalization

把原始單位映射到固定範圍,讓每個關節在網路眼中規模一致。不做的話,
行程 3000 counts 的關節會在 loss 裡壓過行程 300 counts 的那個。

校準的產物就是每個關節的 `min`/`max`(把關節推到機械硬限位掃出來的)。

**範例** —— 某關節 `min=1000`、`max=3000`,當前原始值 `2500`:

```
映射到 [-1, 1]:   2 × (2500−1000) / (3000−1000) − 1  =  2 × 0.75 − 1  =  0.5
映射到 [0, 100]:      (2500−1000) / (3000−1000) × 100 =  75
```

另一種常見做法是 mean/std —— 以 `mean=2000`、`std=500` 為例:

```
(2500 − 2000) / 500 = 1.0        # 「比平均高一個標準差」
```

⚠️ **min-max 同時決定零點「和」增益。** 如果真正的硬限位在 3000,你只掃到 2800,
那所有數值都會被乘上 `2000/1800 = 1.11` —— 這是 11% 的**增益**誤差,不只是偏移。
這就是為什麼重跑校準會讓既有資料集直接作廢。

---

# 3. Relative, Delta, and Absolute Action

同一條軌跡的三種寫法。設當前狀態 `s = 10`,未來三步的目標是:

| 編碼方式 | 數值 | 意思 |
| --- | --- | --- |
| **Absolute(絕對)** | `[12, 15, 19]` | 移動到這個位置 |
| **Relative(相對)** | `[2, 5, 9]` | 相對**當前狀態**的偏移(`aₜ − s`) |
| **Delta(增量)** | `[2, 3, 4]` | 相對**前一步**的偏移(`aₜ − aₜ₋₁`) |

還原方式:

```
relative → absolute:  s + rₜ            10+2, 10+5, 10+9   = 12, 15, 19
delta    → absolute:  s + cumsum(dₜ)    10+2, 12+3, 15+4   = 12, 15, 19
```

差別在哪:

- **Absolute** —— 沒有歧義、永遠不會漂移,但它的分佈取決於你在工作空間的哪個位置。
  同一個動作在不同地點看起來是完全不同的目標值,網路必須先隱含地學會定位。
- **Relative** —— 同一個動作永遠對應同一組數字,分佈集中,訓練收斂得快。
  每個新 chunk 都重新錨定在實測狀態上,所以誤差不會跨 chunk 累積。
- **Delta** —— 分佈最集中,但誤差會**複利**:錯一步之後全部跟著偏,
  因為每一步是相對於「前一次的預測」而不是相對於「量到的值」。

經驗法則:連續定位(手臂關節)適合 relative;絕對值本身就是語義的
(夾爪的開/關)應該維持 absolute。

---

# 4. Prediction Horizon vs Action Horizon

- **Prediction horizon `P`** —— 模型一次推論吐出幾步。訓練時就由資料格式定死。
- **Action horizon `A`** —— 這幾步裡你實際執行幾步才重新規劃。推論時才決定,`A ≤ P`,
  剩下的 `P − A` 步直接丟棄。

**範例** —— `P = 20`、`A = 5`、控制率 10 Hz(每步 0.1 秒):

```
每次推論執行 = 5 步 × 0.1 秒 = 0.5 秒
丟棄         = 20 − 5 = 15 步
30 秒一集的推論次數 = 30 / 0.5 = 60 次
```

同一個模型改成 `A = 20`(全部執行完):

```
每 20 × 0.1 = 2.0 秒才重新規劃 → 只推論 15 次,但中間有 2 秒是瞎的
```

`A` 越小越靈敏、算力越貴;`A = P` 最省、最遲鈍。**模型完全一樣**,變的只有執行策略。

---

# 5. Hyperparameters

## 5.1 GLOBAL_BATCH_SIZE

一次參數更新看幾個樣本,而且是**總量**,會分攤到各張卡。

```
GLOBAL_BATCH_SIZE = 128, 4 張卡  → 每張卡 32 個樣本
資料集 8000 個樣本               → 8000 / 128 = 62.5 次更新/epoch
```

batch 越大,單次更新的梯度估得越準,但更新次數越少。資料集小的時候這個交易很快就不划算:
同樣 8000 個樣本用 batch 640,一個 epoch 只剩 12.5 次更新。

## 5.2 MAX_STEPS

**參數更新次數**,不是 epoch 數。用 batch size 換算:

```
8000 樣本 / batch 128 = 62.5 步/epoch
MAX_STEPS = 10000     → 10000 / 62.5 = 160 個 epoch
```

通常會搭配 cosine schedule,讓學習率剛好在 `MAX_STEPS` 退火到 0 ——
所以跑到一半改這個值,resume 會接不回去。

## 5.3 怎麼決定 batch size 和 MAX_STEPS

**簡答:不是直接設 epoch,但 epoch 是你真正該盯的量。** batch 和 steps 是旋鈕,epoch 是它們的乘積:

```
看過的樣本總數 = MAX_STEPS × GLOBAL_BATCH_SIZE
epoch 數       = MAX_STEPS × GLOBAL_BATCH_SIZE / 資料集樣本數
每 epoch 步數  = 資料集樣本數 / GLOBAL_BATCH_SIZE
```

**單看 steps 完全沒有意義**,因為同一個 `20k steps` 在不同資料集上差好幾個數量級。
拿 NVIDIA 自己 repo 裡的官方 recipe 算:

| 官方 recipe | batch × steps | 資料集規模 | 換算 epoch |
| --- | --- | --- | --- |
| LIBERO(四個 suite) | 640 × 20K | libero_goal ≈ 52K frames | **≈ 246** |
| DROID | 640 × 20K | 數百萬 frames | **< 1** |
| RoboCasa | 512 × 60K | 大 | 少 |

同樣 20K steps,一邊是掃資料集 246 次,一邊連一次都不到。所以**「跑幾 steps」不能直接抄**,
要抄的是換算後的 epoch 數。

### 為什麼 VLA 的 epoch 數比一般模型高這麼多

OpenVLA 論文講得最直接:

> *Typical LLM or VLM training runs complete at most one or two epochs through their training
> dataset. In contrast, we found it important for VLA training to iterate through the training
> dataset significantly more times.*

他們最終的 pretrain 跑了 **27 epochs**(batch 2048,64 張 A100 × 14 天)。
LIBERO 的 fine-tune recipe 換算更是接近 250 epochs。這跟 LLM 微調「1–3 epoch 就過擬合」的直覺完全相反 ——
機器人資料的資訊密度低、相鄰影格高度相關,要重複很多次才學得起來。

實務上常見的區間:**fine-tune 20–100 epochs 起跳**,資料很少時可以到數百。

### 選 batch size

兩個相反的約束夾出答案:

1. **上限是顯存**,以及「更新次數不能太少」。batch 越大,每 epoch 的更新次數越少:

```
5,000 樣本, batch 640 → 每 epoch 只有 7.8 次更新
5,000 樣本, batch 128 → 每 epoch 39 次更新
```

2. **下限是梯度雜訊**。太小的 batch 梯度估計不穩,而且 GPU 跑不滿。

**經驗法則:讓「每 epoch 更新次數」落在數十到數百之間。** 太少 → 一個 epoch 只動幾下,
等於在做超大 batch 的粗糙訓練;太多 → 浪費算力。

GR00T 論文自己就這樣調過:大資料集用 `1024`,但 DexMimicGen 那組因為
**每個 embodiment 的資料量少,改用 `128`**。資料少就把 batch 調小,這是官方做法不是偏方。

### 選 MAX_STEPS

batch 定了之後,**由目標 epoch 數反推**:

```
MAX_STEPS = 目標 epoch × 資料集樣本數 / GLOBAL_BATCH_SIZE
```

**範例** —— 資料集 10,000 樣本,batch 128,想跑 100 epochs:

```
10000 / 128 = 78 步/epoch
78 × 100 = 7,800  → MAX_STEPS ≈ 8000
```

⚠️ 學習率通常是 cosine 退火到 `MAX_STEPS` 歸零,所以這個值**開跑前就要定死**,
中途改會讓排程錯位、resume 接不回去。寧可先用小 `MAX_STEPS` 試,不要跑到一半改。

### 換成自己的機器人(new embodiment)時

pre-train 資料裡沒有你的機器人,所以模型要重新學一組全新的 state/action 映射,
這比「同一台機器人換任務」難。三個實務點:

- **資料量比你想的少就夠**。OpenVLA 顯示 10 個 demo 就有明顯改善,π₀ 級的做法是每個任務
  50–200 個 demo,常見設定是 100 個。先收 50–100 集,不要一開始就衝 1000 集。
- **資料少 → batch 調小、epoch 調高**。照上面的規則走,不要沿用大資料集 recipe 的 batch。
- **co-training 是最有效的正則化**。把目標資料跟預訓練分佈的資料混著訓,可以避免
  catastrophic forgetting,而且 cross-embodiment 資料本身就有正則化效果,
  壓抑對小資料集的過擬合。這比單純減少 epoch 數有效。

### 什麼時候該停

不要靠猜,靠指標:

- **OpenVLA 的做法**:一路訓到 training action token accuracy 超過 **95%** —— 他們觀察到
  在那之前真機成功率還會持續上升。
- **一般做法**:盯 validation loss。跟 training loss 分岔就是過擬合了。
- **VLA 特有**:對一批 held-out 軌跡算 open-loop MSE,每隔幾千步存一個 checkpoint 逐個評。
  MSE 持續下降 = 還在學,走平 = 收斂,回升 = 過擬合。
- ⚠️ **loss 低 ≠ 成功率高**。最終排名一定要看實際 rollout 的成功率,
  loss 和 MSE 只能用來篩掉明顯沒學好的 checkpoint。

## 5.4 DS_WEIGHTS_ALPHA

多個資料集一起訓練時的混合權重。取樣機率正比於 `Lᵢ^α`,`Lᵢ` 是第 i 個資料集的大小
(這裡是它的 shard 數)。

**範例** —— 資料集 A 有 8 個 shard,資料集 B 有 2 個:

| α | 權重 | p(A) | 行為 |
| --- | --- | --- | --- |
| `1` | `8, 2` | `8/10` = **80%** | 正比於大小 |
| `0` | `1, 1` | `1/2` = **50%** | 等比例,完全不看大小 |
| `-1` | `1/8, 1/2` | `0.125/0.625` = **20%** | 反比 —— 偏袒小的那個 |

想要「不管各來源蒐集到多少資料,就是要均衡混合」時,`α = 0` 是好用的預設。

## 5.5 DATALOADER_NUM_WORKERS

餵**每一張** GPU 的 CPU 行程數。太少會餓死 GPU,太多會把 CPU 拖垮。

```
32 vCPU, 4 張卡 → 32/4 = 8 個/rank,扣掉 rank 自己的主行程 = 7
```

設錯的症狀:GPU 使用率上下震盪,而不是穩定貼在 100%。

## 5.6 SHARD_SIZE

一個資料檔裝幾個樣本。shard 數 = `ceil(總數 / SHARD_SIZE)`:

```
10,000 個樣本,SHARD_SIZE = 1024
→ ceil(10000 / 1024) = 10 個 shard,最後一個裝 10000 − 9×1024 = 784 個
```

要知道這個是因為 **§5.4 的權重就是拿 shard 數去算的**。刪掉幾集可能讓你從 10 個 shard
掉到 9 個,資料混合比例就被靜默改掉了。

## 5.7 EPISODE_SAMPLING_RATE

名字很像取樣率,但它**不會丟掉任何資料**。它決定每一集在裝進 shard 之前被切多細:
每集的時間步會先打亂,再交錯地分成 `1/rate` 個子序列。

```
rate = 0.1 → 1/0.1 = 10 個子序列/集
一集 500 步 → 切成 10 段各約 50 步,500 步全部都還在
```

目的是**讓單一 shard 內部夠多樣** —— 一個 shard 裝的是很多集的碎片,
而不是一整段連續影格,這樣從它抽出來的 batch 才不會全是幾乎一樣的畫面。

---

# 6. Open-loop vs Closed-loop Control

- **Open-loop(開迴路)** —— 照計畫執行,不看結果。快,但任何擾動都要等計畫跑完才會被修正。
- **Closed-loop(閉迴路)** —— 觀測、動作、再觀測。會自我修正,代價是每一步都要一次「觀測+思考」。

在 chunk 式的 policy 裡這不是二選一,而是一個旋鈕 —— 就是 §4 的 `A`:

**範例**(控制率 10 Hz):

```
A = 20 → 兩次觀測之間有 2.0 秒開迴路
A = 5  → 0.5 秒
A = 1  → 0.1 秒,完全閉迴路,但推論成本是 A=20 的 20 倍
```

**失效情境** —— 目標物在 chunk 開始後 0.3 秒移動了:

```
A = 20:你會抱著過期的計畫再執行 1.7 秒
A = 1 :下一個 0.1 秒就修正了
```

陷阱是推論不免費。如果一次推論要 0.15 秒而控制週期只有 0.1 秒,`A = 1` 根本跟不上,
手臂每一步都會停頓。`A` 的實務下限是 `推論時間 × 控制率`。
